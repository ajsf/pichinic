import React from 'react'
import Helmet from 'react-helmet'
import Layout from '../../components/layout'

const Cart = props => (
  <Layout>
    <div>
      <Helmet>
        <title>Cart - Forty by HTML5 UP</title>
        <meta name="description" content="Elements Page" />
      </Helmet>
    </div>
  </Layout>
)

export default Cart

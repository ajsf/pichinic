import React from 'react'

const ShoppingCategoryContainer = props => {
  return (
    <section id="two" className="spotlights">
      {props.children}
    </section>
  )
}

export default ShoppingCategoryContainer
